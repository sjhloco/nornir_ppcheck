hosts:
  # HME-C3560-SWI01:
  AZ-ASR-WAN01:
    # run_cfg: True
    cmd_print:
      - show clock
      - show ip int brief
      # - show interface status
      # - show ip route vrf BLU
      # - show ip route
      # - show bgp all summary